"""Kafka producer utilities for publishing security events."""


async def publish_event(topic: str, payload: dict) -> None:
    """Placeholder for future aiokafka producer publishing."""
    _ = (topic, payload)
